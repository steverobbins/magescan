filter:
    excluded_paths:
        - 'bin/*'
        - 'tests/*'
checks:
    php: true
coding_style:
    php: {  }
build_failure_conditions:
    - 'elements.rating(<= C).new.exists'
    - 'issues.new.exists'
